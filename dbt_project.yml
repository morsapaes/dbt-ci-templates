name: 'mz_get_started'
version: '1.0.0'
config-version: 2

profile: 'materialize'

model-paths: ['models']

target-path: 'target'
clean-targets:
  - 'target'
  - 'dbt_modules'

tests:
  materialize:
    +store_failures: true
    +schema: 'etl_failure'
